---
phase: 01-foundations-and-visual-direction
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - data_sections_3.js
autonomous: false
requirements:
  - FOUND-03
must_haves:
  truths:
    - "A reader landing on `kingdom/index.html` (the Kingdom hub) sees a `cta-link` block in the kingdom-overview section that takes them to `kingdom/atlas`."
    - "A reader landing on `kingdom/geography.html` sees an inline 'View the atlas →' link in the geography intro section that takes them to `kingdom/atlas`."
    - "Neither entry-point pre-empts Phase 5's children-grid card upgrade (POLISH-04) — the `cta-link` is a different block from `children-grid`, and no card has been added to the kingdom hub's children-grid block."
  artifacts:
    - path: "data_sections_3.js"
      provides: "kingdom-overview section gains a cta-link block; kingdom/geography intro gains an inline link"
      contains: "type: 'cta-link'"
  key_links:
    - from: "kingdom-overview cta-link block"
      to: "kingdom/atlas"
      via: "the `to:` field of the cta-link block"
      pattern: "to:\\s*'kingdom/atlas'"
    - from: "kingdom/geography intro section"
      to: "kingdom/atlas"
      via: "[[kingdom/atlas:View the atlas]] inline link macro"
      pattern: "\\[\\[kingdom/atlas:"
---

<objective>
Add the two locked entry-point links from CONTEXT.md D-12 / D-16 so readers can find the atlas through normal site navigation.

(1) A `cta-link` block at the bottom of the kingdom-overview section (in `data_sections_3.js`, the section that renders on `kingdom/index.html`).

(2) An inline "View the atlas →" link in `kingdom/geography.html`'s intro section (also in `data_sections_3.js`).

**Critical scope boundary:** NO children-grid card on `kingdom/index.html` is added in this plan. That's Phase 5's POLISH-04 deliverable. Adding it now would step on that scope.

Purpose: The atlas page exists (Plan 03) but is currently undiscoverable without typing the URL. This plan wires it into the two entry points the user locked during discuss-phase.

Output: `data_sections_3.js` modified in two places — a new `cta-link` block appended to the `kingdom-overview` section's `blocks` array, and a new sentence containing `[[kingdom/atlas:View the atlas]]` added to the geography intro section's prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-and-visual-direction/01-CONTEXT.md
@.planning/phases/01-foundations-and-visual-direction/01-03-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@data_sections_3.js
@page_configs.js

<interfaces>
<!-- The cta-link block schema (from CONVENTIONS.md § Block Types). -->
<!-- Already used elsewhere on the site, e.g. campaign hub linking to campaign/hook. -->

cta-link block:
```js
{
  type: 'cta-link',
  to: 'page-key',          // page key as registered in SV_PAGES (NOT a URL)
  kicker: 'Continue to',   // small uppercase eyebrow line
  label: 'The Hook',       // the big title-like display text
  subtitle: '...',         // italic line under the label; supports [[id:Label]] syntax
}
```

Renders as a large title-like block-link with hover violet glow (style.css). Routing follows linkHref()'s page-key resolution.

<!-- The [[id:Label]] inline syntax (from ARCHITECTURE.md § Internal-link micro-syntax + CONVENTIONS.md). -->
<!-- Inside a `p`/`ul`/`table`/`facts`/`defs`/`subtitle` text string, use [[entity-id:Display Label]] for internal links. -->
<!-- For page-key links (linking to a different page entirely, not a section anchor), use the same syntax with the page-key as id: [[kingdom/atlas:View the atlas]]. -->
<!-- linkHref() resolves the id via entityToPage map; for page keys present in SV_PAGES, this routes correctly. -->

<!-- The kingdom-overview section is owned by data_sections_3.js (per the file split: file 3 = hub overviews per CONVENTIONS.md). -->
<!-- The kingdom/geography page's intro section is also in data_sections_3.js (verify by reading the file). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate the kingdom-overview section and the kingdom/geography intro section in data_sections_3.js</name>
  <read_first>
    - data_sections_3.js (read the whole file — it is small enough to read in one pass per CONVENTIONS.md)
    - page_configs.js (verify which section ids the `'kingdom'` page and the `'kingdom/geography'` page list in their `sections: [...]` arrays)
  </read_first>
  <files>
    (read-only — no file modifications in this task)
  </files>
  <action>
This is a discovery task. Output: a short report (printed to the executor's working notes, not committed) with the exact line numbers of:

1. The `kingdom-overview` section's opening line (the `{ id: 'kingdom-overview', ... }` start brace).
2. The `kingdom-overview` section's `blocks: [...]` array opening and closing line numbers.
3. The kingdom/geography intro section's opening line (look in `page_configs.js` `'kingdom/geography'` entry's `sections: [...]` array — the FIRST section id is the intro; e.g. it might be `'geography-intro'` or `'kingdom-geography-overview'` or similar. Find that id's section in `data_sections_3.js`).
4. Within the geography intro section's `blocks: [...]` array, the FIRST `{ type: 'p', text: '...' }` block — that's the intro paragraph the inline link will be added to.

Do NOT modify any file in this task. Just produce the line-number report so Task 2 and Task 3 know exactly where to insert.

**Sanity check:** if the section ids in `page_configs.js` don't resolve to actual entries in `data_sections_3.js`, surface as a blocker — the existing wiki is already broken in that case and Plan 04 cannot proceed without resolution.
  </action>
  <verify>
    <automated>grep -n "id: 'kingdom-overview'" data_sections_3.js | wc -l | awk '{exit !($1 == 1)}'</automated>
  </verify>
  <done>The executor has identified, with file:line references, exactly where the cta-link block will be inserted (Task 2) and exactly where the geography intro inline link will be inserted (Task 3). No files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Append a cta-link block to the kingdom-overview section's blocks array</name>
  <read_first>
    - data_sections_3.js (the kingdom-overview section, lines identified in Task 1)
    - .planning/codebase/CONVENTIONS.md § Block Types (the cta-link schema)
  </read_first>
  <files>data_sections_3.js</files>
  <action>
Open `data_sections_3.js`. Locate the `kingdom-overview` section's `blocks: [...]` array (line numbers from Task 1).

Append this exact block as the LAST element of the `blocks: [...]` array:

```js
{
  type: 'cta-link',
  to: 'kingdom/atlas',
  kicker: 'Open the map',
  label: 'Atlas of K\'naan',
  subtitle: 'A craft-quality SVG map of the Kingdom — visual direction in development.',
},
```

**Constraints:**
- This block goes at the END of the existing `blocks: [...]` array, AFTER any existing blocks (so it reads as the closing call-to-action of the kingdom-overview).
- DO NOT modify any existing block in `kingdom-overview`. DO NOT remove or reorder existing blocks. The children-grid block (if one exists in this section already) stays exactly where it is — Phase 5's POLISH-04 will modify it later, NOT this plan.
- Indentation matches the surrounding entries (read the file to confirm; likely 4 spaces inside the blocks array).
- The trailing comma after the closing brace is required IF this is not the last block in the array. If this becomes the last block (which it will, per the action above), match the file's existing convention — most data_sections_*.js files have trailing commas, but verify by reading the immediate context.
- Subtitle is **Phase-1-honest by design**. The atlas page in Phase 1 only shows a static sketch snippet (per Plan 03's standalone shell embedding the locked sketch); pan/zoom and "every place named" are Phase 3 deliverables. The subtitle "visual direction in development" matches what the page actually shows on click-through, without overpromising features that ship later. A future phase (Phase 5 POLISH-04) replaces this cta-link with a full children-grid card; at that point the subtitle can be re-revised to its full-feature-promising form. Phase 1 stays honest about its current state.
- The subtitle is plain prose (no `[[id:Label]]` macros) and contains no GM-only Averium content. User reviews on commit.
- The `to:` field uses the page key `'kingdom/atlas'` (not a URL like `'kingdom/atlas.html'`) — page keys are how `cta-link.to` resolves through `linkHref`.

**Verification commands the executor should run:**
- `grep -c "to: 'kingdom/atlas'" data_sections_3.js` must return at least `1`.
- `node -e "var w = {}; (function(){var window=w; eval(require('fs').readFileSync('data_sections_3.js', 'utf8'))})(); console.log('parses ok');"` must succeed (syntactic validity).

**No new prose authored beyond the cta-link's subtitle:** the kicker, label, and subtitle text above is the ONLY prose this task adds. No additional sentences, no descriptions of place, no transitions. The subtitle is a UI affordance, not lore.
  </action>
  <verify>
    <automated>grep -c "to: 'kingdom/atlas'" data_sections_3.js | awk '{exit !($1 >= 1)}' &amp;&amp; node -e "var w = {}; (function(){var window=w; eval(require('fs').readFileSync('data_sections_3.js', 'utf8'))})();"</automated>
  </verify>
  <done>data_sections_3.js parses as valid JavaScript; the kingdom-overview section's blocks array contains a new `{ type: 'cta-link', to: 'kingdom/atlas', ... }` block as its last entry; no existing blocks were modified or reordered; no children-grid card was added to the kingdom hub.</done>
</task>

<task type="auto">
  <name>Task 3: Add the inline 'View the atlas →' link to the kingdom/geography intro section</name>
  <read_first>
    - data_sections_3.js (the geography intro section, lines identified in Task 1)
    - .planning/codebase/ARCHITECTURE.md § Internal-link micro-syntax (the `[[id:Label]]` resolution chain)
    - .planning/codebase/CONVENTIONS.md § Internal-Link Convention (especially "Don't link the same entity in every paragraph" — first mention is the strong link)
  </read_first>
  <files>data_sections_3.js</files>
  <action>
Open `data_sections_3.js`. Locate the geography intro section's `blocks: [...]` array. Find the FIRST `{ type: 'p', text: '...' }` block (the opening prose paragraph).

Append a new sentence to the END of that paragraph's `text` string:

```
[[kingdom/atlas:View the atlas →]]
```

The result should look like (example — the actual existing text will differ):

```js
// BEFORE:
{ type: 'p', text: 'The Kingdom of K\'naan stretches from the Silt-Mere in the northwest to the Eastern Massif. ...existing prose...' },

// AFTER:
{ type: 'p', text: 'The Kingdom of K\'naan stretches from the Silt-Mere in the northwest to the Eastern Massif. ...existing prose... [[kingdom/atlas:View the atlas →]]' },
```

**Constraints:**
- The `[[kingdom/atlas:View the atlas →]]` is appended at the END of the existing first-paragraph text string, separated by a single space from the prior sentence.
- DO NOT rewrite the existing prose. DO NOT add a transition sentence. DO NOT change punctuation in the existing text. Only append the inline link macro at the end.
- The display label is "View the atlas →" exactly (with the unicode right-arrow `→`, U+2192). This matches CONTEXT.md D-16's locked copy.
- The page key `kingdom/atlas` is used (not `kingdom/atlas.html`). `app.js`'s `linkHref` resolves page keys via `entityToPage` and `SV_PAGES` lookup.
- DO NOT add the link to any other paragraph, list, or block in the section. ONE link only, in the FIRST paragraph.
- DO NOT add a new entry to `tooltips.js` for `kingdom/atlas` (page-key links don't need tooltips; page keys aren't tooltip ids — tooltips are keyed by entity/section ids).

**Verification commands the executor should run:**
- `grep -c '\[\[kingdom/atlas:View the atlas' data_sections_3.js` must return exactly `1`.
- `node -e "var w = {}; (function(){var window=w; eval(require('fs').readFileSync('data_sections_3.js', 'utf8'))})();"` must succeed.
- Open `kingdom/geography.html` in a browser; confirm the intro paragraph now ends with a clickable "View the atlas →" link in gold; click it; confirm it navigates to `kingdom/atlas.html`.

**No new prose authored:** the only text added is the link macro `[[kingdom/atlas:View the atlas →]]`. The display label "View the atlas →" is UI affordance copy, not lore prose. The existing geography intro paragraph is left exactly as it was.
  </action>
  <verify>
    <automated>grep -c '\[\[kingdom/atlas:View the atlas' data_sections_3.js | awk '{exit !($1 == 1)}' &amp;&amp; node -e "var w = {}; (function(){var window=w; eval(require('fs').readFileSync('data_sections_3.js', 'utf8'))})();"</automated>
  </verify>
  <done>data_sections_3.js parses as valid JavaScript; exactly one `[[kingdom/atlas:View the atlas →]]` macro exists in the file (specifically in the geography intro section's first paragraph); no other prose was modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke-test both entry points in a browser</name>
  <what-built>
- A `cta-link` block at the bottom of the `kingdom-overview` section, taking readers from `kingdom/index.html` to `kingdom/atlas.html`. Subtitle reads "A craft-quality SVG map of the Kingdom — visual direction in development." (Phase-1-honest about the page's current sketch-snippet state; Phase 5 POLISH-04 will replace this cta-link with a full children-grid card and may revise the subtitle then.)
- A `[[kingdom/atlas:View the atlas →]]` inline link in the geography intro paragraph, taking readers from `kingdom/geography.html` to `kingdom/atlas.html`.
- No children-grid card was added (that's POLISH-04's deliverable).
  </what-built>
  <how-to-verify>
1. Serve the repo (`python3 -m http.server 8000` from the repo root) and open `http://localhost:8000/kingdom/index.html`.
2. Scroll to the bottom of the kingdom-overview section. Verify a `cta-link` block is rendered with kicker "OPEN THE MAP" (uppercase Cinzel), label "Atlas of K'naan" (UnifrakturCook), and subtitle italic prose reading "A craft-quality SVG map of the Kingdom — visual direction in development." Hover — verify the violet glow on hover (style.css `.sv-cta-link:hover` rule).
3. Click the cta-link. Verify it navigates to `kingdom/atlas.html`. Verify the atlas page renders correctly (the locked sketch snippet from Plan 03 should be visible). Confirm the subtitle's promise ("visual direction in development") matches what the reader sees on the atlas page — a craft-direction-locked sketch snippet, not yet a navigable map.
4. Use browser back-button. Verify nothing else on `kingdom/index.html` changed — specifically, verify the children-grid (if it exists) has the SAME cards it had before this plan ran. NO new "Atlas" card should appear in it.
5. Open `http://localhost:8000/kingdom/geography.html`. Read the intro paragraph. Verify it now ends with a clickable "View the atlas →" link styled as a gold inline link. Click it. Verify it navigates to `kingdom/atlas.html`.
6. Open `http://localhost:8000/kingdom/index.html` again. Open browser DevTools Console. Verify no red errors (no broken `[[…]]` resolution warnings, no missing-section errors).
  </how-to-verify>
  <resume-signal>Type "approved" if both entry points work and no children-grid card was added. If anything fails, describe the failure and Claude will fix.</resume-signal>
  <acceptance_criteria>
    - User has explicitly approved or flagged a failure
    - cta-link from kingdom/index.html → kingdom/atlas.html works
    - Inline link from kingdom/geography.html → kingdom/atlas.html works
    - No children-grid card was inadvertently added (Phase 5 POLISH-04 scope is intact)
    - Browser console shows zero red errors on both kingdom/index.html and kingdom/geography.html
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data_sections_3.js edit → kingdom hub + geography page | Both pages source content from this file; a syntax error breaks both |
| `[[kingdom/atlas:View the atlas →]]` macro → linkHref() resolution | The page-key form must resolve via SV_PAGES['kingdom/atlas'] (added in Plan 03) — if Plan 03 wasn't run yet, this link breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Syntax error in cta-link block insertion breaks the kingdom hub render (silent blank page from app.js error path) | mitigate | Task 2 verify gate runs `node -e ... eval(...)` on data_sections_3.js to confirm syntactic validity. Task 4 smoke-test re-loads kingdom/index.html and confirms render |
| T-04-02 | Information Disclosure | cta-link subtitle leaks Averium-only content | accept | Subtitle is generic ("A craft-quality SVG map of the Kingdom — visual direction in development."), no GM-only references; user reviews on commit |
| T-04-03 | Tampering | Inline link macro `[[kingdom/atlas:…]]` is malformed (e.g. mismatched brackets) and breaks renderInline() for the entire geography intro paragraph | mitigate | Task 3 verify gate confirms exactly one well-formed macro via grep; Task 4 smoke-test confirms the geography page renders and the link is clickable |
| T-04-04 | Scope Creep | Executor pre-emptively adds a children-grid card for the atlas (would conflict with Phase 5 POLISH-04) | mitigate | Task 2 action explicitly forbids modifying the children-grid block. Task 4 smoke-test step 4 verifies the children-grid is unchanged. CONTEXT.md D-12 is the locked decision: cta-link block, NOT a card |
</threat_model>

<verification>
- `data_sections_3.js` parses as valid JavaScript (no syntax errors)
- `kingdom-overview` section has exactly one new `cta-link` block with `to: 'kingdom/atlas'`
- The geography intro section's first paragraph contains exactly one `[[kingdom/atlas:View the atlas →]]` macro
- The kingdom hub's children-grid block (if present) was NOT modified
- Both pages render correctly in a browser with zero console errors
- Both links navigate correctly to `kingdom/atlas.html`
</verification>

<success_criteria>
1. ROADMAP Phase 1 success criterion #1 reinforced: A reader can click into the atlas from `kingdom/index.html` and from `kingdom/geography.html`.
2. FOUND-03 satisfied: A reader can reach the atlas from both entry points.
3. POLISH-04 scope is intact (no children-grid card added).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-visual-direction/01-04-SUMMARY.md` recording:
- The exact line numbers where the cta-link block and the inline link were inserted
- The final wording of the cta-link's kicker / label / subtitle
- A note for Phase 5 POLISH-04: "the kingdom-overview's children-grid block has NOT been modified by Phase 1; the atlas card is yours to add"
</output>
