---
phase: 01-foundations-and-visual-direction
plan: 06
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - .planning/phases/01-foundations-and-visual-direction/01-06-VERIFICATION.md
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "All 4 ROADMAP Phase 1 success criteria have been verified live in a browser, not just by file existence."
    - "All 6 FOUND-** requirements have a verified-passing entry in the verification document."
    - "Any failures are surfaced as gaps for `/gsd-plan-phase 1 --gaps` to close, not silently accepted."
  artifacts:
    - path: ".planning/phases/01-foundations-and-visual-direction/01-06-VERIFICATION.md"
      provides: "Goal-backward verification report covering all 4 success criteria + all 6 requirements"
      contains: "ROADMAP Phase 1 Success Criterion"
  key_links:
    - from: "VERIFICATION.md per-criterion entries"
      to: "live browser-verified state of the site"
      via: "manual smoke-test results recorded with screenshots/notes"
      pattern: "PASS|FAIL"
---

<objective>
Run goal-backward verification against ROADMAP.md Phase 1's 4 success criteria and REQUIREMENTS.md's 6 FOUND-** requirements. The plan is one task: a structured smoke-test pass that exercises every claim Phase 1 makes, in a browser, against the actual deployed-locally state of the site.

Purpose: Phase 1 has 5 prior plans, each of which had its own verification. This plan is the integration test — does the WHOLE phase deliver what ROADMAP and REQUIREMENTS say it should? If yes, Phase 1 closes cleanly and Phase 2 (Lore-Faithful Geography Spec) can begin. If no, gaps are surfaced for `/gsd-plan-phase 1 --gaps` to close before transition.

Output: `.planning/phases/01-foundations-and-visual-direction/01-06-VERIFICATION.md` with a structured PASS/FAIL entry for each success criterion and each requirement, plus a "Gaps to close" section that is empty (full pass) or contains specific gap items (gaps mode required).

This plan does not modify any live-site file. It only produces the verification report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-and-visual-direction/01-CONTEXT.md
@.planning/phases/01-foundations-and-visual-direction/01-01-SUMMARY.md
@.planning/phases/01-foundations-and-visual-direction/01-02-SUMMARY.md
@.planning/phases/01-foundations-and-visual-direction/01-03-SUMMARY.md
@.planning/phases/01-foundations-and-visual-direction/01-04-SUMMARY.md
@.planning/phases/01-foundations-and-visual-direction/01-05-SUMMARY.md

<interfaces>
<!-- ROADMAP.md Phase 1 success criteria (must verify each LIVE, not by file inspection alone): -->
<!-- 1. Reader can type kingdom/atlas.html (or click in from kingdom/index.html and kingdom/geography.html) and land on a full-bleed, chrome-free page that loads with no JS errors. -->
<!-- 2. User has previewed 5+ distinct stylistic variants from /gsd-sketch round, picked a winner (or rejected and re-sketched), and chosen visual register is documented in .planning/sketches/MANIFEST.md. -->
<!-- 3. Hovering silveldt, the-drift, the-rot, third-furnace, tide-reach-wharf, or anvilhold anywhere on the existing wiki produces a real tooltip and clicks resolve to a live section anchor (no dead #id fallbacks). -->
<!-- 4. The atlas page is registered as a child of the Kingdom hub in page_configs.js (hub: 'kingdom', parent: 'kingdom') so site-nav users can find it through normal info architecture. -->

<!-- REQUIREMENTS.md FOUND-** requirements: -->
<!-- FOUND-01: kingdom/atlas.html exists and loads without JS errors -->
<!-- FOUND-02: page is registered as a child of the Kingdom hub in page_configs.js -->
<!-- FOUND-03: a reader can reach the atlas from kingdom/index.html (children grid) and from kingdom/geography.html — NOTE: the original requirement says "children grid" but CONTEXT.md D-12 explicitly DEFERS the children-grid card to Phase 5 POLISH-04, replacing it with a cta-link block in Phase 1. Verification accepts cta-link as the Phase 1 path; the children-grid is Phase 5's job. -->
<!-- FOUND-04: full-bleed, no archbar/sidebar/breadcrumbs/footer chrome competing with the map; standalone-cinematic pattern -->
<!-- FOUND-05: visual register locked via /gsd-sketch round (5+ variants), user picked winner, MANIFEST.md documents -->
<!-- FOUND-06: 6 atlas-relevant orphan [[id:Label]] references resolved with real section entries OR alias indirection (per CONTEXT.md D-14 two-mechanism rule) — verified by tooltips firing and clicks resolving to live anchors -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the goal-backward verification pass and record results</name>
  <what-built>
All 5 prior Phase 1 plans (01-01 sketch, 01-02 audit, 01-03 page-shell, 01-04 entry-point links, 01-05 orphan repairs) have completed. The site should now satisfy all 4 ROADMAP success criteria and all 6 FOUND-** requirements.
  </what-built>
  <how-to-verify>
This is a structured smoke-test. The executor (or user) walks through every check below and records results live in `.planning/phases/01-foundations-and-visual-direction/01-06-VERIFICATION.md`.

Setup: serve the repo (`python3 -m http.server 8000` from the repo root). Open a browser to `http://localhost:8000/`.

**Verification doc structure** (executor creates this file as the smoke-test progresses; do NOT pre-populate with PASS — every entry must be verified):

```markdown
# Phase 1 — Verification Report

**Verified:** <today's ISO date>
**Verifier:** <human user via Claude smoke-test>
**Outcome:** <PASS / GAPS / FAIL>

## ROADMAP Phase 1 Success Criteria

### Criterion 1 — atlas.html loads with no chrome and no errors
- [ ] kingdom/atlas.html resolves at `http://localhost:8000/kingdom/atlas.html` (HTTP 200)
- [ ] Page is full-bleed black; no archbar, sidebar, breadcrumbs, footer
- [ ] Browser DevTools Console shows zero red errors
- [ ] Locked sketch's snippet is visible centered on the page
- [ ] "Return to The Kingdom" anchor appears top-left and clicks navigate to kingdom/index.html
- **Status:** PASS / FAIL
- **Notes:** <any observations>

### Criterion 2 — visual register locked via sketch round
- [ ] `.planning/sketches/MANIFEST.md` exists
- [ ] MANIFEST.md contains a `**Chosen register:**` line with a register name
- [ ] MANIFEST.md contains a `**Locked file:**` line pointing at an existing .svg in `.planning/sketches/`
- [ ] At least 5 distinct .svg files exist in `.planning/sketches/`
- [ ] User's verbatim rationale is preserved in the Lock-In section
- **Status:** PASS / FAIL
- **Notes:** <any observations>

### Criterion 3 — 6 orphan refs resolve to live anchors
For each of: silveldt, the-drift, the-rot, third-furnace, tide-reach-wharf, anvilhold:
  - [ ] Find a wiki page that mentions the orphan (use `grep -lE "<display-name>" *.html data_sections_*.js`)
  - [ ] On that page in the browser: hover the orphan's display name. Tooltip appears (Mechanism A: orphan's tooltip OR no-op-but-not-error; Mechanism B: parent's tooltip)
  - [ ] Click the orphan's display name. URL bar fragment is a real section id (Mechanism A: `#<orphan-id>` with rendered content below; Mechanism B: `#<parent-id>` with the parent's section content visible). NO dead `#orphan-id` with no rendered content.
- **Status:** PASS / FAIL per orphan
  - silveldt: PASS / FAIL
  - the-drift: PASS / FAIL
  - the-rot: PASS / FAIL
  - third-furnace: PASS / FAIL
  - tide-reach-wharf: PASS / FAIL
  - anvilhold: PASS / FAIL
- **Notes:** <any observations>

### Criterion 4 — atlas registered as child of Kingdom hub in page_configs.js
- [ ] `grep -c "'kingdom/atlas':" page_configs.js` returns exactly 1
- [ ] The entry has `hub: 'kingdom'`
- [ ] The entry has `parent: 'kingdom'`
- [ ] The entry has `href: 'kingdom/atlas.html'`
- [ ] The entry has `standalone: true`
- [ ] kingdom/index.html and kingdom/places.html still render correctly (page_configs.js wasn't broken by the new entry)
- **Status:** PASS / FAIL
- **Notes:** <any observations>

## REQUIREMENTS — Per-Requirement Verification

### FOUND-01: kingdom/atlas.html exists and loads without JS errors
- Status: PASS / FAIL (see Criterion 1)

### FOUND-02: page registered in page_configs.js
- Status: PASS / FAIL (see Criterion 4)

### FOUND-03: reader can reach atlas from kingdom hub AND kingdom/geography.html
- [ ] Browse to `http://localhost:8000/kingdom/index.html`. Verify a `cta-link` block at the bottom of the kingdom-overview section. Click it. Confirm navigation to kingdom/atlas.html.
- [ ] Browse to `http://localhost:8000/kingdom/geography.html`. Verify the geography intro paragraph ends with "View the atlas →" as a clickable gold link. Click it. Confirm navigation to kingdom/atlas.html.
- [ ] Verify NO children-grid card was added to kingdom/index.html (Phase 5 POLISH-04 scope intact).
- **Status:** PASS / FAIL
- **Notes:** Per CONTEXT.md D-12, Phase 1 uses cta-link not children-grid; the original FOUND-03 wording mentions "children grid" but the CONTEXT.md decision overrides — accept cta-link as Phase 1 fulfillment; children-grid is Phase 5.

### FOUND-04: full-bleed, no chrome
- Status: PASS / FAIL (see Criterion 1)

### FOUND-05: visual register locked via 5+ sketch round
- Status: PASS / FAIL (see Criterion 2; original requirement said 3-4, CONTEXT.md D-01 raised to 5+, verification uses the 5+ count)

### FOUND-06: 6 orphans resolved
- Status: PASS / FAIL (see Criterion 3)

## Cross-Cutting Hard-Rule Checks

These are project-wide constraints that Phase 1's plans must not have violated:

- [ ] **No third-party CDN deps beyond Google Fonts.** `grep -lE "(cdnjs\|jsdelivr\|unpkg\|vanta\|three\.js\|gsap\|fontawesome)" kingdom/atlas.html` returns nothing.
- [ ] **No new prose authored.** Diff `git diff data.js data_sections_*.js tooltips.js page_configs.js` and verify every added line of human-readable text traces to a `// lifted verbatim from <source>` comment OR is structural (block schema, alias entry, entry registration). Specifically for orphan repairs: zero lines of authored description.
- [ ] **No animation libraries introduced.** `grep -lE "(animate\|gsap\|three\|vanta\|lottie)" kingdom/atlas.html` returns nothing (the locked sketch's SVG should be static; static `<animate>` tags should NOT be present per Plan 01's static-only constraint).
- [ ] **Cache-bust unchanged at `?v=25` for site-wide files.** `grep -c "?v=25" kingdom/index.html kingdom/geography.html kingdom/places.html` matches the pre-Phase-1 count (the Phase 5 bump to `?v=26` is POLISH-03's job).
- [ ] **No Averium leakage.** `grep -lE "Averium\|Hellig DND" data_sections_*.js data.js tooltips.js page_configs.js` returns no files (or only files where Averium is mentioned as a worldbuilding context that was already there pre-Phase-1).
- **Status:** PASS / FAIL per row above

## Gaps to Close

(If everything above is PASS, this section is empty and Phase 1 is complete. If any item is FAIL, list each as a structured gap below for `/gsd-plan-phase 1 --gaps` to consume.)

### Gap N: <one-line summary>
- **Failed criterion or requirement:** <e.g. "FOUND-06 / silveldt">
- **Observed state:** <what actually happens in the browser>
- **Expected state:** <what should happen per ROADMAP/REQUIREMENTS/CONTEXT>
- **Likely fix scope:** <which file(s), which mechanism, est. context cost>

## Outcome

- **PASS** — all criteria and requirements verified live; cross-cutting hard rules upheld; no gaps.
- **GAPS** — most items pass but specific items failed; gaps section above lists each; `/gsd-plan-phase 1 --gaps` will close.
- **FAIL** — fundamental architecture issue (e.g. atlas.html doesn't exist, page_configs.js is broken site-wide); requires intervention beyond a gap-closure plan.
```

Walk through every checkbox in order. Do NOT mark PASS without verifying live in the browser. If a checkbox fails, mark FAIL on that line, capture observed state, and add a corresponding entry to the Gaps section.

After completing all checks, write the file to `.planning/phases/01-foundations-and-visual-direction/01-06-VERIFICATION.md` and set the top-level **Outcome:** marker.
  </how-to-verify>
  <resume-signal>
Type "approved" if Outcome is PASS and the verification document is complete. If Outcome is GAPS, the user can either run `/gsd-plan-phase 1 --gaps` immediately to close them, or accept the gaps as known issues to defer (in which case STATE.md must record the deferral with rationale). If Outcome is FAIL, the user and Claude must triage at the conversation level before proceeding.
  </resume-signal>
  <acceptance_criteria>
    - .planning/phases/01-foundations-and-visual-direction/01-06-VERIFICATION.md exists
    - File contains all 4 ROADMAP Phase 1 success criteria with PASS/FAIL status
    - File contains all 6 FOUND-** requirements with PASS/FAIL status
    - File contains the Cross-Cutting Hard-Rule Checks section with PASS/FAIL status
    - File ends with an Outcome marker (PASS / GAPS / FAIL)
    - If Outcome is GAPS, each gap has a structured entry suitable for `/gsd-plan-phase 1 --gaps` to consume
    - If Outcome is PASS, ROADMAP.md Phase 1 can be marked complete
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke-tester → verification report | A premature PASS hides a real failure and ships broken state to Phase 2. The discipline is to verify live, not by file existence alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Verifier marks PASS without actually opening the browser, missing console errors or visual regressions | mitigate | Task 1's how-to-verify steps are explicit about browser-required checks (DevTools Console, hover behavior, click navigation). The acceptance criteria require live verification, not file inspection |
| T-06-02 | Information Disclosure | Verification document quotes new-prose lines that violate the lift-don't-embellish rule | mitigate | Cross-Cutting Hard-Rule Checks section explicitly diffs and verifies no authored prose appears. Surfaces violations as gaps |
| T-06-03 | Repudiation | A FAIL is silently rewritten as PASS to close Phase 1 faster | mitigate | The Outcome marker is the user's signal to GSD. If the user types "approved" with the document showing FAIL items, that's a deliberate user decision (recorded in STATE.md as a deferral with rationale, not silent acceptance) |
</threat_model>

<verification>
- VERIFICATION.md exists in the phase dir
- All 4 success criteria, all 6 requirements, all cross-cutting checks have a recorded status
- Outcome is one of PASS / GAPS / FAIL
- If GAPS, every gap is structured for `/gsd-plan-phase 1 --gaps` consumption
- If PASS, the user has explicitly approved the report
</verification>

<success_criteria>
1. Phase 1 has a complete verification report on disk
2. The user has acknowledged the verification outcome (approved PASS, accepted GAPS for `--gaps` mode, or escalated FAIL)
3. Phase 1's transition to Phase 2 has a green light (PASS) OR a documented deferral path (GAPS with rationale)
4. ROADMAP.md Phase 1 checkbox can be marked `[x]` if PASS
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-visual-direction/01-06-SUMMARY.md` recording:
- The verification outcome (PASS / GAPS / FAIL)
- A list of any gaps surfaced
- Any cross-cutting hard-rule violations observed
- A pointer to VERIFICATION.md for full details
- A recommendation: proceed to `/gsd-transition` (PASS) / `/gsd-plan-phase 1 --gaps` (GAPS) / triage at conversation level (FAIL)
</output>
