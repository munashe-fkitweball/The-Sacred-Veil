---
phase: 01-foundations-and-visual-direction
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - kingdom/atlas.html
  - page_configs.js
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04
must_haves:
  truths:
    - "A reader can type `kingdom/atlas.html` in the address bar and land on a full-bleed page that loads with no JS errors in the console."
    - "The page is full-bleed: no archbar, no sidebar, no breadcrumbs, no footer chrome."
    - "The page does NOT load `app.js`, `style.css`, `data*.js`, `tooltips.js`, or `page_configs.js`."
    - "The page contains the locked sketch's snippet inline as authored SVG."
    - "A small 'return to Kingdom' anchor exists in a corner of the page."
    - "`window.SV_PAGES['kingdom/atlas']` exists in `page_configs.js` with `hub: 'kingdom'`, `parent: 'kingdom'`, `standalone: true`, and the minimum required fields."
  artifacts:
    - path: "kingdom/atlas.html"
      provides: "Standalone-cinematic atlas page shell with the locked sketch's snippet embedded"
      min_lines: 60
      contains: "Return to The Kingdom"
    - path: "page_configs.js"
      provides: "SV_PAGES['kingdom/atlas'] entry registering the atlas page"
      contains: "'kingdom/atlas':"
  key_links:
    - from: "kingdom/atlas.html return anchor"
      to: "../kingdom/index.html"
      via: "direct relative href, NOT [[id:Label]] syntax"
      pattern: "href=\"\\.\\./kingdom/index\\.html\""
    - from: "page_configs.js SV_PAGES['kingdom/atlas']"
      to: "kingdom hub"
      via: "hub: 'kingdom', parent: 'kingdom' fields"
      pattern: "hub:\\s*'kingdom'"
---

<objective>
Create the standalone-cinematic shell at `kingdom/atlas.html` and register it in `page_configs.js`. The page mirrors `party/index.html`'s shape (inline `<style>`, inline `<script>`, no engine load), embeds the locked sketch's snippet from Plan 01, and provides a small return-to-Kingdom anchor. The `page_configs.js` entry exists so the wider site's breadcrumbs/search corpus know about the atlas, but the atlas page itself doesn't depend on it at runtime.

Purpose: This is the structural container Phases 3-5 will fill in. Phase 1 only delivers the shell + the embedded sketch snippet so the page exists in the right place with the right architecture. Phase 3 will replace the snippet with the full atlas SVG.

Output: `kingdom/atlas.html` (new file), `page_configs.js` (one new entry appended cleanly without disturbing existing entries).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-and-visual-direction/01-CONTEXT.md
@.planning/phases/01-foundations-and-visual-direction/01-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@party/index.html
@page_configs.js

<interfaces>
<!-- The canonical standalone-cinematic precedent (CONTEXT.md "Specific Ideas"). -->
<!-- kingdom/atlas.html copies the structural shape, NOT the Vanta/Three.js dependencies. -->

party/index.html shape:
- &lt;!doctype html&gt; &lt;html lang="en"&gt; &lt;head&gt; ... &lt;/head&gt; &lt;body&gt; ... &lt;/body&gt; &lt;/html&gt;
- &lt;head&gt; contains: &lt;meta charset&gt;, &lt;meta viewport&gt;, &lt;title&gt;, OG/Twitter meta block (between &lt;!-- OG_START --&gt; and &lt;!-- OG_END --&gt;), favicon link, Google Fonts &lt;link rel="preconnect"&gt; + stylesheet for Cinzel/Cormorant Garamond/UnifrakturCook, INLINE &lt;style&gt; block (no external style.css)
- &lt;body&gt; contains: a thin "← Back" anchor (top-left corner), the page content, an INLINE &lt;script&gt; block (no external app.js / data*.js / tooltips.js)
- Does NOT set window.SV_CURRENT_PAGE
- Does NOT load page_configs.js (party/index.html has no runtime dependence on it)

page_configs.js SV_PAGES['party'] entry (page_configs.js:315-322 per ARCHITECTURE.md):
```js
'party': {
  hub: 'party',
  href: 'party/index.html',
  numeral: 'VIII',
  title: 'The Party',
  subtitle: 'Those who walk into the dark',
  standalone: true,
}
```

The 'standalone: true' flag is documentation-only — app.js does not consume it.
</interfaces>

<locked_entry_template>
The exact `SV_PAGES` entry to add (under hub II Kingdom, alphabetically grouped with the existing kingdom/* entries):

```js
'kingdom/atlas': {
  hub: 'kingdom',
  parent: 'kingdom',
  href: 'kingdom/atlas.html',
  title: 'Atlas of K\'naan',
  subtitle: 'A craft-quality SVG map of the Kingdom',
  standalone: true,
},
```

Subtitle is Claude's draft per CONTEXT.md "Claude's discretion." User reviews on commit. If user prefers a different subtitle, swap it before commit.
</locked_entry_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kingdom/atlas.html as standalone-cinematic shell with embedded locked sketch</name>
  <read_first>
    - .planning/phases/01-foundations-and-visual-direction/01-01-SUMMARY.md (to learn which sketch file is the locked winner)
    - .planning/sketches/MANIFEST.md § Lock-In (to confirm the locked file path)
    - .planning/sketches/&lt;locked-filename&gt;.svg (the actual SVG content to embed)
    - party/index.html (the canonical standalone shape — copy structural patterns, NOT Vanta/Three.js)
    - .planning/codebase/CONVENTIONS.md § Visual Language Tokens (for inline-style palette + fonts)
  </read_first>
  <files>kingdom/atlas.html</files>
  <action>
Create `kingdom/atlas.html` as a NEW file (the file does not yet exist; verify with `ls kingdom/atlas.html 2>/dev/null` returning nothing before writing).

The file MUST have this structure:

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Atlas of K'naan — The Sacred Veil</title>

<!-- OG_START -->
<meta name="description" content="A craft-quality SVG atlas of the Kingdom of K'naan in the World of Averium.">
<meta property="og:title" content="Atlas of K'naan — The Sacred Veil">
<meta property="og:description" content="A craft-quality SVG atlas of the Kingdom of K'naan in the World of Averium.">
<meta property="og:type" content="article">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Atlas of K'naan — The Sacred Veil">
<meta name="twitter:description" content="A craft-quality SVG atlas of the Kingdom of K'naan in the World of Averium.">
<!-- OG_END -->

<link rel="icon" type="image/png" href="../assets/sigil.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;500;600;700&family=Cormorant+Garamond:ital,wght@0,400;0,500;1,400&family=UnifrakturCook:wght@700&display=swap">

<style>
  /* Locked palette + fonts mirroring style.css :root, but inline so this page is self-contained. */
  :root {
    --gold: #d8c99b;
    --gold-bright: #f2d98c;
    --gold-dim: rgba(200,160,80,0.55);
    --gold-faint: rgba(200,160,80,0.20);
    --bg: #060309;
    --bg-deep: #030106;
    --purple: rgba(95,55,130,0.30);
    --text: #f0e4bc;
    --text-dim: rgba(240,228,188,0.85);
    --border: rgba(180,140,70,0.25);
  }
  * { box-sizing: border-box; }
  html, body { margin: 0; padding: 0; background: var(--bg-deep); color: var(--text); font-family: 'Cormorant Garamond', serif; }
  body { min-height: 100vh; overflow: hidden; }

  /* Full-bleed atlas surface */
  .atlas-stage {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-deep);
  }
  .atlas-stage svg {
    width: 100%;
    height: 100%;
    max-width: 100vw;
    max-height: 100vh;
  }

  /* Return-to-Kingdom anchor (top-left corner per CONTEXT.md D-09) */
  .atlas-return {
    position: fixed;
    top: 24px;
    left: 24px;
    z-index: 10;
    font-family: 'Cinzel', serif;
    font-size: 12px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--gold-dim);
    text-decoration: none;
    padding: 8px 14px;
    border: 1px solid var(--border);
    background: rgba(6, 3, 9, 0.6);
    transition: color 200ms ease, border-color 200ms ease;
  }
  .atlas-return:hover,
  .atlas-return:focus-visible { color: var(--gold-bright); border-color: var(--gold-dim); outline: none; }

  /* Phase 1 placeholder note (small, subtle, bottom-right) — explains this is the snippet, not the full atlas yet. */
  .atlas-phase-note {
    position: fixed;
    bottom: 16px;
    right: 20px;
    font-family: 'Cormorant Garamond', serif;
    font-style: italic;
    font-size: 12px;
    color: var(--text-dim);
    opacity: 0.55;
    pointer-events: none;
  }

  /* Reduced motion guard — even though Phase 1 has no animation, future-proof. */
  @media (prefers-reduced-motion: reduce) {
    * { animation: none !important; transition: none !important; }
  }

  /* Noscript: this page is mostly static SVG so it works fine without JS, but include the convention. */
  .atlas-noscript {
    position: fixed;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gold);
    font-family: 'Cinzel', serif;
    text-align: center;
    padding: 40px;
  }
</style>
</head>

<body>
<noscript>
  <div class="atlas-noscript">
    The Atlas is best viewed with JavaScript enabled, but the map itself is static SVG and should still render.
  </div>
</noscript>

<a class="atlas-return" href="../kingdom/index.html">← Return to The Kingdom</a>

<div class="atlas-stage">
  <!-- LOCKED SKETCH SNIPPET — Phase 1 placeholder. Phase 3 replaces this with the full atlas. -->
  <!-- Source: .planning/sketches/<LOCKED-FILE>.svg (Plan 01-01 lock-in) -->
  <!--
    INSERT VERBATIM the entire contents of the locked sketch SVG file here,
    starting with its outer <svg ...> opening tag and ending with </svg>.
    Do NOT modify the SVG contents. Do NOT add scripts to the SVG.
    Do NOT remove the <title> element from the SVG.
    The outer <svg>'s viewBox is preserved so the snippet scales cleanly inside .atlas-stage.
  -->
</div>

<div class="atlas-phase-note">Phase 1 — visual register placeholder. Full atlas in Phase 3.</div>

<script>
  'use strict';
  // Phase 1 has no atlas interaction yet. Pan/zoom is Phase 3's responsibility.
  // This inline script exists to match party/index.html's standalone shape and to allow
  // simple future hooks (e.g. keyboard "Esc to return") without reaching for app.js.
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') {
      var a = document.querySelector('.atlas-return');
      if (a) window.location.href = a.getAttribute('href');
    }
  });
</script>
</body>
</html>
```

**Critical insertion step:** in the `<div class="atlas-stage">` block, replace the comment block (`<!-- INSERT VERBATIM ... -->`) with the verbatim contents of the locked sketch SVG file. To find the locked file:

1. Read `.planning/sketches/MANIFEST.md` and find the `**Locked file:** <filename>.svg` line.
2. Read `.planning/sketches/<filename>.svg` in full.
3. Insert the entire contents of that file (from `<svg ...>` through `</svg>`) into the `.atlas-stage` div, replacing the placeholder comment.

**Constraints:**
- The file MUST NOT include `<script src="../app.js">`, `<script src="../data.js">`, `<script src="../data_sections_1.js">`, `<script src="../data_sections_2.js">`, `<script src="../data_sections_3.js">`, `<script src="../tooltips.js">`, `<script src="../page_configs.js">`, or `<link rel="stylesheet" href="../style.css">`. Verify by grep after writing.
- The file MUST NOT set `window.SV_CURRENT_PAGE`. Verify by grep.
- The file MUST NOT add a third-party CDN dependency beyond Google Fonts (Google Fonts is already site-wide convention). NO Vanta, Three.js, GSAP, FontAwesome, etc. Verify by grep for `cdnjs`, `cdn.jsdelivr`, `unpkg`, etc.
- The locked sketch's SVG content is inserted VERBATIM — do not edit, simplify, or reformat it.
- Cache-bust handling: this page does not load shared scripts, so no `?v=NN` query strings are needed (CONTEXT.md D-10). Do not add any.

**Asset path note:** `../assets/sigil.png` and `../kingdom/index.html` are correct relative paths from `kingdom/atlas.html` (one directory deep, so `../` reaches the repo root). Verify the favicon and return-link paths resolve.
  </action>
  <verify>
    <automated>test -f kingdom/atlas.html &amp;&amp; ! grep -E '(app\.js|data\.js|data_sections|tooltips\.js|page_configs\.js|style\.css|SV_CURRENT_PAGE|cdnjs|jsdelivr|unpkg|vanta|three\.js|gsap|fontawesome)' kingdom/atlas.html &amp;&amp; grep -q 'Return to The Kingdom' kingdom/atlas.html &amp;&amp; grep -q '&lt;svg' kingdom/atlas.html &amp;&amp; grep -q 'href="\.\./kingdom/index\.html"' kingdom/atlas.html &amp;&amp; grep -q 'noscript' kingdom/atlas.html</automated>
  </verify>
  <done>kingdom/atlas.html exists; does NOT load app.js / style.css / data*.js / tooltips.js / page_configs.js; does NOT set SV_CURRENT_PAGE; does NOT pull any third-party CDN dep beyond Google Fonts; contains an inline &lt;svg&gt; block (the locked sketch's verbatim contents); contains a "Return to The Kingdom" anchor pointing at ../kingdom/index.html; contains a &lt;noscript&gt; fallback; contains an inline &lt;style&gt; block; contains an inline &lt;script&gt; block.</done>
</task>

<task type="auto">
  <name>Task 2: Add SV_PAGES['kingdom/atlas'] entry to page_configs.js</name>
  <read_first>
    - page_configs.js (entire file — read it in one pass to find the right insertion point and confirm no syntax surprises)
    - .planning/codebase/CONCERNS.md § "page_configs.js is a single point of failure" (the warning to add the entry cleanly without disturbing unrelated entries)
    - .planning/codebase/ARCHITECTURE.md § Component Responsibilities (the SV_PAGES schema)
  </read_first>
  <files>page_configs.js</files>
  <action>
Open `page_configs.js`. Locate the section that holds the `'kingdom/...'` entries (e.g. `'kingdom'`, `'kingdom/geography'`, `'kingdom/places'`, `'kingdom/peoples'`, `'kingdom/people'`).

Add a new entry, placed alphabetically among the kingdom/* entries (between `'kingdom'` (the hub root) and `'kingdom/geography'`, OR wherever the alphabetical position falls — `atlas` < `geography` < `people` < `peoples` < `places`). Read the file to find the exact insertion line.

The exact entry to add (verbatim):

```js
  'kingdom/atlas': {
    hub: 'kingdom',
    parent: 'kingdom',
    href: 'kingdom/atlas.html',
    title: 'Atlas of K\'naan',
    subtitle: 'A craft-quality SVG map of the Kingdom',
    standalone: true,
  },
```

**Constraints:**
- Match the surrounding indentation exactly (likely 2 spaces, but read the file to confirm — if the file uses 4 spaces or tabs, match that instead).
- Match the surrounding quote style (likely single quotes for keys and string values; the file's existing style is canonical).
- Do NOT modify any OTHER entry. Do NOT reorder existing entries. Do NOT add or remove blank lines around unrelated entries. Per CONCERNS.md, a typo here breaks every wiki page.
- After adding, the closing `};` of the `window.SV_PAGES = {...}` object must remain syntactically valid (i.e. the new entry has a trailing comma since it's not the last entry, OR if it ends up as the last entry, no trailing comma if the file's existing style omits trailing commas).
- The `'kingdom/atlas'` key must be unique. Grep before AND after to confirm no duplicate.

**Verification commands the executor should run before declaring done:**
- `grep -c "'kingdom/atlas':" page_configs.js` must return `1` (not 0, not 2+).
- `node -e "var w = {}; (function(){var window=w; eval(require('fs').readFileSync('page_configs.js', 'utf8'))})(); console.log(JSON.stringify(w.SV_PAGES['kingdom/atlas']));"` must successfully print the new entry as JSON. (This is a syntax-validity smoke test — if `page_configs.js` is broken, this command throws.)

**Subtitle disclaimer per CONTEXT.md "Claude's discretion":** the subtitle "A craft-quality SVG map of the Kingdom" is Claude's draft. User reviews on commit; if user prefers a different subtitle (e.g. "The Kingdom of K'naan, drawn in primitives"), swap it before commit.
  </action>
  <verify>
    <automated>grep -c "'kingdom/atlas':" page_configs.js | awk '{exit !($1 == 1)}' &amp;&amp; node -e "var w = {}; (function(){var window=w; eval(require('fs').readFileSync('page_configs.js', 'utf8'))})(); var p = w.SV_PAGES['kingdom/atlas']; if (!p) throw new Error('missing entry'); if (p.hub !== 'kingdom') throw new Error('wrong hub'); if (p.parent !== 'kingdom') throw new Error('wrong parent'); if (p.href !== 'kingdom/atlas.html') throw new Error('wrong href'); if (p.standalone !== true) throw new Error('missing standalone flag');"</automated>
  </verify>
  <done>page_configs.js contains exactly one `'kingdom/atlas':` entry; the entry has `hub: 'kingdom'`, `parent: 'kingdom'`, `href: 'kingdom/atlas.html'`, a `title`, a `subtitle`, and `standalone: true`; the file still parses as valid JavaScript (the node smoke-test passes); no other entries were modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke-test the atlas page in a browser</name>
  <what-built>
- `kingdom/atlas.html` exists as a full-bleed standalone page embedding the locked sketch's snippet.
- `page_configs.js` has a new `'kingdom/atlas'` entry with `hub: 'kingdom'`, `parent: 'kingdom'`, `standalone: true`.
- The page loads with no engine dependence (no `app.js`, no `style.css`, no `data*.js`, no `tooltips.js`, no `page_configs.js` runtime load).
  </what-built>
  <how-to-verify>
1. Open `kingdom/atlas.html` directly in a browser by `file://` URL or by serving the repo (e.g. `python3 -m http.server 8000` from the repo root, then visit `http://localhost:8000/kingdom/atlas.html`).
2. Open the browser DevTools Console. Verify there are NO red errors. (Yellow warnings about missing favicon paths if served from a different root are acceptable.)
3. Visually verify:
   - The page is full-bleed black (`--bg-deep`), no archbar at the top, no sidebar, no breadcrumbs, no footer.
   - The locked sketch's snippet is visible centered in the viewport.
   - A small "← RETURN TO THE KINGDOM" anchor in Cinzel uppercase sits in the top-left corner.
   - A small italic note "Phase 1 — visual register placeholder. Full atlas in Phase 3." sits subtly in the bottom-right corner.
4. Click the return anchor. Verify it navigates to `kingdom/index.html` (the existing wiki page).
5. Press the Esc key. Verify it also navigates to `kingdom/index.html` (per the inline JS keyboard hook).
6. Re-load existing wiki pages (`kingdom/index.html`, `kingdom/places.html`) and confirm they still render correctly — that nothing in `page_configs.js` was broken by the new entry.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. If anything fails (console error, missing return-link, broken existing wiki page), describe the failure and Claude will fix.</resume-signal>
  <acceptance_criteria>
    - User has explicitly approved by typing "approved" or has flagged a failure
    - If failure flagged, Claude has produced a fix and the smoke test is repeated
    - Browser console shows zero red errors when loading kingdom/atlas.html
    - kingdom/index.html and kingdom/places.html still render correctly (page_configs.js wasn't broken)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| atlas.html → cross-link to ../kingdom/index.html | A direct relative anchor, not engine-resolved; safe (no `[[id:Label]]` macro running on this standalone page) |
| page_configs.js edit → entire wiki | Per CONCERNS.md, this file is a single point of failure for the wiki. A typo breaks every page |
| sketch SVG → atlas.html embedded content | Sketch was hand-authored by Claude in Plan 01; verified static and primitives-only by Plan 01's Task 3 verify gate. Embedding it inline does not introduce new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | A typo in page_configs.js breaks every wiki page (the renderer aborts when SV_PAGES key is missing) | mitigate | Task 2 verify gate runs the file through `node -e ... eval(...)` to confirm syntactic validity. Task 3 smoke test re-loads kingdom/index.html and kingdom/places.html to confirm site-wide intact |
| T-03-02 | Elevation of Privilege (values violation) | atlas.html accidentally pulls in a third-party CDN dependency (e.g. fontawesome.css), violating PROJECT.md "no third-party libs" hard rule | mitigate | Task 1 verify gate explicitly greps for `cdnjs\|jsdelivr\|unpkg\|vanta\|three\.js\|gsap\|fontawesome` and rejects if any match. Google Fonts is the only allowed external dependency (already site-wide convention) |
| T-03-03 | Tampering | Embedded sketch SVG contains a `<script>` tag that executes when atlas.html loads | mitigate | Plan 01 Task 3 verify gate already excluded `<script>` from sketches. Task 1 of this plan additionally instructs the executor not to add scripts to the SVG. Task 3 smoke test verifies no console errors on page load |
| T-03-04 | Information Disclosure | Subtitle in page_configs.js entry leaks GM-only Averium content | accept | Subtitle is Claude-drafted and reviewed by user on commit; the draft "A craft-quality SVG map of the Kingdom" is generic and contains no GM-only references |
| T-03-05 | Denial of Service | Inlined sketch SVG is so large that atlas.html exceeds the 1.5MB hard cap from PROJECT.md | accept | Sketches are diagnostic-scale (~80KB cap from Plan 01). Even if all of one is inlined, atlas.html stays well under the cap. Phase 3 will replace the snippet with the full atlas and at THAT point the cap matters |
</threat_model>

<verification>
- `kingdom/atlas.html` exists and loads in a browser with no JS errors
- `kingdom/atlas.html` does NOT reference `app.js`, `style.css`, `data*.js`, `tooltips.js`, or `page_configs.js`
- `kingdom/atlas.html` does NOT pull in third-party CDN deps beyond Google Fonts
- `kingdom/atlas.html` is full-bleed (no archbar, sidebar, breadcrumbs, footer)
- `kingdom/atlas.html` contains the locked sketch's snippet inline
- `kingdom/atlas.html` has a return-to-Kingdom anchor pointing at `../kingdom/index.html`
- `page_configs.js` contains exactly one `'kingdom/atlas':` entry with the locked field set
- `page_configs.js` still parses as valid JavaScript (existing wiki pages unbroken)
</verification>

<success_criteria>
1. ROADMAP Phase 1 success criterion #1 satisfied: A reader can type `kingdom/atlas.html` and land on a full-bleed, chrome-free page with no JS errors.
2. ROADMAP Phase 1 success criterion #4 satisfied: The atlas page is registered as a child of the Kingdom hub in `page_configs.js` (`hub: 'kingdom'`, `parent: 'kingdom'`).
3. FOUND-01, FOUND-02, FOUND-04 covered.
4. The locked sketch's visual register is now visible at the production URL `kingdom/atlas.html`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-visual-direction/01-03-SUMMARY.md` recording:
- The locked sketch file that was embedded
- The exact `SV_PAGES['kingdom/atlas']` entry that was added
- The user-approved subtitle (or note if Claude's draft was kept)
- Any browser-console warnings observed during smoke test
- A pointer to which Phase 3 plan will replace the embedded snippet with the full atlas
</output>
